# TODO.md

> Reconstructed from conversation history. Items marked `[confirm]` require verification in the actual repository before taking action.

## Pending — explicitly postponed by the user/previous agent

* [ ] **Repository `README.md`** — mentioned as the next step, not yet created at the time these documents were generated.
* [ ] **Major dependency updates** (`fastapi`, `starlette`, `uvicorn`, `alembic`, `python-jose`) — deliberately postponed due to risk of breaking changes; only small patches (`argon2-cffi`, `email-validator`, `httpx`, `python-multipart`, `slowapi`, `websockets`, `pip`) were updated. Recorded recommendation: do this in isolation, with testing time, near a future deployment cycle — not mixed with another task.
* [ ] **Migrate from `python-jose` to `PyJWT`** — mentioned as a future maintainability improvement (`python-jose` has a slower maintenance cadence), not urgent (current use with HS256 + explicit algorithm in decode is not exposed to algorithm confusion vulnerability).
* [ ] **`description_en` field per project** — implemented, but depends on the user manually filling it in for each project (there is no automatic translation). Confirm whether all registered projects have the English version filled in.
* [ ] **Refresh token — no "connected devices"/manual revocation UI** — not implemented; scope deliberately simplified because this is a single-user system.

## Risks / Technical Cleanup

* [ ] **Confirm whether the temporary diagnostic log in `app/core/turnstile.py`** (`security_logger.warning("turnstile_verify_failed cloudflare_response=%s", result)`) was kept or removed — it was deliberately added to debug an `invalid-input-secret` error, without an explicit instruction to remove it afterward. Check whether this level of logging is still desirable in production (it may log tokens/response data that should not remain in persistent logs depending on Railway's retention policy).
* [ ] **Confirm real vs. test Turnstile keys in all environments** — throughout the conversation there were repeated switches between the test key (`1x00000000000000000000AA`) and the real key (`0x4AAAAAAD2-bMzcMMaQWBgk`) in `frontend/index.html`, `frontend/assets/js/config.js`, `frontend/painel-fg-2026x/login.html`, and `backend/.env`/Railway. Confirm that **all 4 locations** use the same key (real, for actual production) — inconsistencies between them cause `invalid-input-secret` errors or prevent the widget from loading.
* [ ] **Admin password backup** — the user entered the admin password in plain text in the chat on at least one occasion (via a URL, when a form accidentally submitted using GET). It was recommended that the password be changed afterward; **confirm whether this was done** — there is no way to verify this from the code, since the actual password is not visible (only the hash would be stored in the database).
* [ ] **Alembic installed but not used** — `requirements.txt` includes `alembic==1.13.3`, but all schema migrations were performed via ad-hoc scripts (`add_*_column.py`) + `create_all()`. If the project grows, it would be worth migrating to proper versioned migrations.

## Possible Next Steps (not explicitly requested, but natural continuation)

* [ ] Populate `description_en` for all existing projects (if not already done)
* [ ] Confirm the Turnstile hostname registered in the Cloudflare dashboard includes both `localhost` (dev) and the real Vercel domain (production)
* [ ] Consider hosting the flag GIFs (Brazil/USA) locally instead of hotlinking from Giphy, for the same reason project images were migrated from Imgur — risk of the link breaking/being blocked
* [ ] Custom domain — the project is currently running on free subdomains (`vercel.app`, `up.railway.app`). If the user purchases a custom domain, the following will need to be updated: `CORS_ALLOWED_ORIGINS`, `ALLOWED_HOSTS`, hostname in Turnstile, and the `og:url`/`og:image`/`twitter:image` meta tags in `index.html`
* [ ] `MISE_PYTHON_GITHUB_ATTESTATIONS=false` is set on Railway as a workaround for an infrastructure failure on their side (Python signature verification) — this is not one of our security settings, but a workaround for a third-party bug; reassess whether it is still necessary in the future

## Completed in This Development Session (for reference — do not redo)

See `CHANGELOG_AI.md` for the complete history. Summary: complete public site (hero, about, skills, projects, contact), complete admin panel (CRUD + MFA + statistics), extensive security checklist implemented and verified, "Blueprint" visual identity with Brazil/USA accents, functional production deployment (Railway + Vercel + Neon).